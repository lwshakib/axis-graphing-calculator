# AXIS Graphing Calculator - Environment Configuration Example

# THEME: Default theme for the application (light/dark)
NEXT_PUBLIC_DEFAULT_THEME=dark

# DATABASE: Prisma connection string for PostgreSQL
# Replace 'user', 'password', 'localhost', '5432', and 'dbname' with your actual credentials.
DATABASE_URL="postgresql://user:password@localhost:5432/axis_db?schema=public"

# AUTHENTICATION: Better Auth Secret
# Generate a random string using: openssl rand -base64 32
BETTER_AUTH_SECRET=your_base64_secret_here

# AUTHENTICATION: Base URL for authentication callbacks
BETTER_AUTH_URL=http://localhost:3000

# SOCIAL PROVIDERS: Google OAuth Credentials
# Create these at: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# APP CONFIG: Public Base URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000
